import { AppRegistry } from 'react-native';
// import List from './List';
import Section from './Section';

// AppRegistry.registerComponent('PropsState', () => List);
AppRegistry.registerComponent('PropsState', () => Section);
